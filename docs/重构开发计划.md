# OpenClaw Deploy 重构开发计划

> **版本**: 2.0  
> **日期**: 2026-02-05  
> **目标**: 智能一键部署方案 - 统一美观的交互体验

---

## 🎯 项目愿景

打造一个**智能、美观、易用**的 OpenClaw 一键部署系统：
- 🧠 **智能检测** - 自动识别环境，智能选择安装模式
- 🎨 **统一界面** - 美观的 TUI 界面，一致的交互体验
- 🔧 **模块化** - 功能独立，易于维护和扩展
- 🌍 **双版本** - 支持国际版和中文版

---

## 📋 核心需求分析

### 用户需求清单

| 序号 | 需求 | 描述 | 优先级 |
|------|------|------|--------|
| 1 | 智能环境检测 | 扫描系统环境，判断全新安装/升级更新 | P0 |
| 2 | 双版本支持 | 国际版 (openclaw) / 中文版 (openclaw-cn) | P0 |
| 3 | 交互式系统配置 | 引导式配置 API Key、频道、模型等 | P0 |
| 4 | 交互式技能管理 | 搜索、安装、管理 Skills | P1 |
| 5 | 交互式软件安装 | 可选依赖软件清单 | P1 |
| 6 | 系统状态检查 | 健康检查、诊断、修复 | P1 |
| 7 | 脚本自更新 | 检查并更新部署脚本 | P2 |

### 设计原则

1. **统一美观** - 所有界面风格一致，专业美观
2. **实时反馈** - 操作进度实时显示，不让用户等待
3. **友好提示** - 错误信息清晰，提供解决方案
4. **智能默认** - 合理的默认值，减少用户输入
5. **可中断恢复** - 支持中断后继续

---

## 🏗️ 系统架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    OpenClaw Deploy 2.0                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   主入口    │  │  UI 框架   │  │  配置管理   │         │
│  │  main.sh   │  │   ui.sh    │  │  config.sh  │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                 │
│  ┌──────┴────────────────┴────────────────┴──────┐         │
│  │                  核心模块层                    │         │
│  ├─────────────────────────────────────────────────┤         │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐       │         │
│  │  │ 环境检测 │ │ 安装管理 │ │ 配置向导 │       │         │
│  │  │detector.sh│ │installer.sh│ │wizard.sh │       │         │
│  │  └──────────┘ └──────────┘ └──────────┘       │         │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐       │         │
│  │  │ 技能管理 │ │ 状态检查 │ │ 自更新   │       │         │
│  │  │skills.sh │ │health.sh │ │updater.sh│       │         │
│  │  └──────────┘ └──────────┘ └──────────┘       │         │
│  └─────────────────────────────────────────────────┘         │
│                                                             │
│  ┌─────────────────────────────────────────────────┐         │
│  │                  工具函数层                      │         │
│  │  utils.sh | logger.sh | network.sh | backup.sh  │         │
│  └─────────────────────────────────────────────────┘         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 文件结构

```
openclaw-deploy/
├── deploy.sh                    # 主入口脚本（单文件可执行）
├── lib/                         # 模块库
│   ├── ui.sh                    # UI 框架
│   ├── utils.sh                 # 工具函数
│   ├── logger.sh                # 日志系统
│   ├── config.sh                # 配置管理
│   ├── detector.sh              # 环境检测
│   ├── installer.sh             # 安装管理
│   ├── wizard.sh                # 配置向导
│   ├── skills.sh                # 技能管理
│   ├── health.sh                # 状态检查
│   ├── updater.sh               # 自更新
│   └── software.sh              # 软件管理
├── templates/                   # 配置模板
│   ├── openclaw.json.tpl        # 主配置模板
│   ├── env.tpl                  # 环境变量模板
│   └── systemd.service.tpl      # 服务模板
├── data/                        # 数据文件
│   ├── software-list.json       # 软件清单
│   └── skills-popular.json      # 热门技能
└── docs/                        # 文档
    └── README.md
```

---

## 🎨 统一 UI 框架设计

### 1. 视觉风格

#### 颜色方案
```bash
# 主色调
PRIMARY="\033[38;5;39m"      # 蓝色 - 主要信息
SUCCESS="\033[38;5;82m"      # 绿色 - 成功
WARNING="\033[38;5;220m"     # 黄色 - 警告
ERROR="\033[38;5;196m"       # 红色 - 错误
INFO="\033[38;5;245m"        # 灰色 - 次要信息
ACCENT="\033[38;5;213m"      # 紫色 - 强调
CYAN="\033[38;5;51m"         # 青色 - 标题

# 样式
BOLD="\033[1m"
DIM="\033[2m"
RESET="\033[0m"
```

#### 主界面 Banner
```
╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║     ██████╗ ██████╗ ███████╗███╗   ██╗ ██████╗██╗      █████╗║
║    ██╔═══██╗██╔══██╗██╔════╝████╗  ██║██╔════╝██║     ██╔══██║
║    ██║   ██║██████╔╝█████╗  ██╔██╗ ██║██║     ██║     ███████║
║    ██║   ██║██╔═══╝ ██╔══╝  ██║╚██╗██║██║     ██║     ██╔══██║
║    ╚██████╔╝██║     ███████╗██║ ╚████║╚██████╗███████╗██║  ██║
║     ╚═════╝ ╚═╝     ╚══════╝╚═╝  ╚═══╝ ╚═════╝╚══════╝╚═╝  ╚═╝
║                                                              ║
║              🦞 智能一键部署系统 v2.0                         ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝
```

### 2. UI 组件库

#### 2.1 进度条
```bash
# 带百分比的进度条
show_progress() {
    local current=$1
    local total=$2
    local width=50
    local percent=$((current * 100 / total))
    local filled=$((width * current / total))
    local empty=$((width - filled))
    
    printf "\r  ["
    printf "%${filled}s" | tr ' ' '█'
    printf "%${empty}s" | tr ' ' '░'
    printf "] %3d%%" "$percent"
}

# 示例效果:
#   [████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░] 40%
```

#### 2.2 旋转加载动画
```bash
# 旋转动画
spinner() {
    local pid=$1
    local message=$2
    local spinstr='⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏'
    
    while kill -0 $pid 2>/dev/null; do
        for i in $(seq 0 9); do
            printf "\r  ${CYAN}${spinstr:$i:1}${RESET} $message"
            sleep 0.1
        done
    done
    printf "\r  ${SUCCESS}✓${RESET} $message\n"
}

# 示例效果:
#   ⠹ 正在检测系统环境...
```

#### 2.3 选择菜单
```bash
# 单选菜单
select_menu() {
    local title=$1
    shift
    local options=("$@")
    local selected=0
    local key
    
    while true; do
        clear_menu
        echo -e "\n  ${BOLD}$title${RESET}\n"
        
        for i in "${!options[@]}"; do
            if [ $i -eq $selected ]; then
                echo -e "  ${PRIMARY}❯ ${options[$i]}${RESET}"
            else
                echo -e "    ${DIM}${options[$i]}${RESET}"
            fi
        done
        
        echo -e "\n  ${INFO}↑/↓ 选择  Enter 确认  q 退出${RESET}"
        
        read -rsn1 key
        case "$key" in
            A) ((selected > 0)) && ((selected--)) ;;  # 上
            B) ((selected < ${#options[@]}-1)) && ((selected++)) ;;  # 下
            '') return $selected ;;  # Enter
            q) return 255 ;;
        esac
    done
}

# 示例效果:
#   选择安装版本
#
#   ❯ 国际版 (openclaw)
#     中文版 (openclaw-cn)
#
#   ↑/↓ 选择  Enter 确认  q 退出
```

#### 2.4 多选菜单
```bash
# 多选菜单（带复选框）
multi_select_menu() {
    local title=$1
    shift
    local options=("$@")
    local selected=()
    local current=0
    
    # 初始化选中状态
    for i in "${!options[@]}"; do
        selected[$i]=0
    done
    
    while true; do
        clear_menu
        echo -e "\n  ${BOLD}$title${RESET}\n"
        
        for i in "${!options[@]}"; do
            local checkbox="[ ]"
            [ ${selected[$i]} -eq 1 ] && checkbox="[✓]"
            
            if [ $i -eq $current ]; then
                echo -e "  ${PRIMARY}❯ $checkbox ${options[$i]}${RESET}"
            else
                echo -e "    ${DIM}$checkbox ${options[$i]}${RESET}"
            fi
        done
        
        echo -e "\n  ${INFO}↑/↓ 移动  Space 选择  Enter 确认  a 全选${RESET}"
        
        read -rsn1 key
        case "$key" in
            A) ((current > 0)) && ((current--)) ;;
            B) ((current < ${#options[@]}-1)) && ((current++)) ;;
            ' ') selected[$current]=$((1 - ${selected[$current]})) ;;
            a) for i in "${!options[@]}"; do selected[$i]=1; done ;;
            '') break ;;
        esac
    done
    
    # 返回选中的索引
    SELECTED_ITEMS=()
    for i in "${!selected[@]}"; do
        [ ${selected[$i]} -eq 1 ] && SELECTED_ITEMS+=($i)
    done
}

# 示例效果:
#   选择要安装的软件
#
#   ❯ [✓] Node.js v22
#     [✓] Git
#     [ ] Docker
#     [✓] ffmpeg
#
#   ↑/↓ 移动  Space 选择  Enter 确认  a 全选
```

#### 2.5 输入框
```bash
# 带默认值的输入框
input_box() {
    local prompt=$1
    local default=$2
    local secret=${3:-false}
    local result
    
    if [ "$secret" = true ]; then
        echo -ne "  ${BOLD}$prompt${RESET}"
        [ -n "$default" ] && echo -ne " ${DIM}[已设置]${RESET}"
        echo -ne ": "
        read -rs result
        echo
    else
        echo -ne "  ${BOLD}$prompt${RESET}"
        [ -n "$default" ] && echo -ne " ${DIM}[$default]${RESET}"
        echo -ne ": "
        read result
    fi
    
    echo "${result:-$default}"
}

# 示例效果:
#   Gateway 端口 [18789]: 
#   API Key [已设置]: 
```

#### 2.6 确认框
```bash
# 确认框
confirm_box() {
    local message=$1
    local default=${2:-n}
    local hint="y/N"
    [ "$default" = "y" ] && hint="Y/n"
    
    echo -ne "  ${WARNING}?${RESET} $message ${DIM}[$hint]${RESET}: "
    read -r answer
    answer=${answer:-$default}
    
    [[ "$answer" =~ ^[Yy]$ ]]
}

# 示例效果:
#   ? 确认开始安装? [Y/n]: 
```

#### 2.7 信息面板
```bash
# 信息面板
info_panel() {
    local title=$1
    shift
    local items=("$@")
    
    echo -e "\n  ┌─────────────────────────────────────────────┐"
    echo -e "  │ ${BOLD}$title${RESET}"
    echo -e "  ├─────────────────────────────────────────────┤"
    
    for item in "${items[@]}"; do
        echo -e "  │   $item"
    done
    
    echo -e "  └─────────────────────────────────────────────┘\n"
}

# 示例效果:
#   ┌─────────────────────────────────────────────┐
#   │ 系统环境检测结果
#   ├─────────────────────────────────────────────┤
#   │   操作系统: Ubuntu 24.04 LTS
#   │   内存: 8192 MB
#   │   Node.js: v22.5.0
#   │   OpenClaw: 未安装
#   └─────────────────────────────────────────────┘
```

#### 2.8 状态指示器
```bash
# 状态图标
status_icon() {
    case "$1" in
        success) echo -e "${SUCCESS}✓${RESET}" ;;
        error)   echo -e "${ERROR}✗${RESET}" ;;
        warning) echo -e "${WARNING}!${RESET}" ;;
        info)    echo -e "${INFO}ℹ${RESET}" ;;
        pending) echo -e "${DIM}○${RESET}" ;;
        running) echo -e "${CYAN}◉${RESET}" ;;
    esac
}

# 日志输出
log_success() { echo -e "  $(status_icon success) $1"; }
log_error()   { echo -e "  $(status_icon error) $1"; }
log_warning() { echo -e "  $(status_icon warning) $1"; }
log_info()    { echo -e "  $(status_icon info) $1"; }

# 示例效果:
#   ✓ Node.js 安装成功
#   ✗ 配置验证失败
#   ! 检测到旧版本配置
#   ℹ 使用默认端口 18789
```

---

## 🧠 模块详细设计

### 模块 1: 智能环境检测 (detector.sh)

#### 功能描述
启动时自动扫描系统环境，智能判断安装模式。

#### 检测项目

```bash
# 检测清单
detect_all() {
    # 1. 操作系统检测
    detect_os()           # Linux/macOS/WSL2
    detect_distro()       # Ubuntu/Debian/CentOS/Arch
    detect_arch()         # x86_64/arm64
    
    # 2. 系统资源检测
    detect_memory()       # 内存大小
    detect_disk()         # 磁盘空间
    detect_cpu()          # CPU 核心数
    
    # 3. 依赖检测
    detect_node()         # Node.js 版本
    detect_npm()          # npm/pnpm
    detect_git()          # Git
    detect_curl()         # curl/wget
    
    # 4. OpenClaw 检测
    detect_openclaw()     # CLI 版本
    detect_config()       # 配置文件
    detect_service()      # 服务状态
    detect_workspace()    # 工作区
    
    # 5. 网络检测
    detect_network()      # 网络连通性
    detect_proxy()        # 代理设置
}
```

#### 安装模式判断

```bash
# 判断安装模式
determine_install_mode() {
    local mode="fresh"  # 默认全新安装
    
    # 检测 OpenClaw CLI
    if command -v openclaw &>/dev/null || command -v openclaw-cn &>/dev/null; then
        local current_version=$(openclaw --version 2>/dev/null || openclaw-cn --version 2>/dev/null)
        local latest_version=$(get_latest_version)
        
        if version_lt "$current_version" "$latest_version"; then
            mode="upgrade"  # 升级安装
        else
            mode="reinstall"  # 重新安装/修复
        fi
    fi
    
    # 检测配置文件
    if [ -f "$HOME/.openclaw/openclaw.json" ]; then
        EXISTING_CONFIG=true
    fi
    
    # 检测服务
    if systemctl is-active --quiet openclaw 2>/dev/null; then
        SERVICE_RUNNING=true
    fi
    
    echo "$mode"
}
```

#### 输出格式

```
┌─────────────────────────────────────────────────┐
│ 🔍 系统环境检测
├─────────────────────────────────────────────────┤
│   操作系统    Ubuntu 24.04 LTS (x86_64)
│   内存        8192 MB ✓
│   磁盘        120 GB 可用 ✓
│   Node.js     v22.5.0 ✓
│   Git         2.43.0 ✓
├─────────────────────────────────────────────────┤
│ 🦞 OpenClaw 状态
├─────────────────────────────────────────────────┤
│   CLI 版本    1.2.3 → 1.3.0 (可升级)
│   配置文件    已存在
│   服务状态    运行中
│   工作区      ~/.openclaw/workspace
├─────────────────────────────────────────────────┤
│ 📋 建议操作: 升级安装
└─────────────────────────────────────────────────┘
```

---

### 模块 2: 安装管理 (installer.sh)

#### 功能描述
支持国际版和中文版的安装，调用官方 install.sh 或手动安装。

#### 安装流程

```bash
# 主安装流程
install_openclaw() {
    local version=$1  # international / chinese
    local mode=$2     # fresh / upgrade / reinstall
    
    # 1. 安装前检查
    pre_install_check
    
    # 2. 安装系统依赖（可选）
    if [ "$INSTALL_DEPS" = true ]; then
        install_system_dependencies
    fi
    
    # 3. 安装 Node.js（如果需要）
    if ! check_node_version; then
        install_nodejs
    fi
    
    # 4. 安装 OpenClaw CLI
    case "$version" in
        international)
            install_openclaw_international "$mode"
            ;;
        chinese)
            install_openclaw_chinese "$mode"
            ;;
    esac
    
    # 5. 配置服务
    if [ "$INSTALL_SERVICE" = true ]; then
        setup_systemd_service
    fi
    
    # 6. 安装后验证
    post_install_verify
}
```

#### 版本选择界面

```
  选择 OpenClaw 版本

  ❯ 🌍 国际版 (openclaw)
      - 官方原版，英文界面
      - 命令: openclaw
      - 源: npm install -g openclaw@latest
    
    🇨🇳 中文版 (openclaw-cn)
      - 中文本地化，支持国产模型
      - 命令: openclaw-cn
      - 源: npm install -g openclaw-cn@latest

  ↑/↓ 选择  Enter 确认
```

#### 安装方式选择

```
  选择安装方式

  ❯ 🚀 快速安装 (推荐)
      使用官方安装脚本，自动配置
    
    📦 手动安装
      仅安装 CLI，手动配置
    
    🔧 自定义安装
      选择要安装的组件

  ↑/↓ 选择  Enter 确认
```

---

### 模块 3: 交互式配置向导 (wizard.sh)

#### 功能描述
引导用户完成 OpenClaw 配置，生成符合官方标准的配置文件。

#### 配置流程

```bash
# 配置向导主流程
run_config_wizard() {
    # 步骤 1: AI Provider 配置
    wizard_step_providers
    
    # 步骤 2: 模型选择
    wizard_step_models
    
    # 步骤 3: 频道配置
    wizard_step_channels
    
    # 步骤 4: Gateway 配置
    wizard_step_gateway
    
    # 步骤 5: 性能优化
    wizard_step_optimization
    
    # 步骤 6: 确认并生成
    wizard_step_confirm
}
```

#### 步骤 1: AI Provider 配置

```
╔══════════════════════════════════════════════════════════════╗
║  配置向导 - 步骤 1/6: AI Provider                            ║
╚══════════════════════════════════════════════════════════════╝

  选择要配置的 AI Provider (可多选)

  ❯ [✓] Anthropic (Claude)
    [✓] OpenAI (GPT)
    [ ] DeepSeek
    [ ] Google (Gemini)
    [ ] 本地模型 (Ollama)

  ↑/↓ 移动  Space 选择  Enter 下一步

───────────────────────────────────────────────────────────────
  配置 Anthropic API Key

  API Key [已设置]: sk-ant-***
  
  ✓ API Key 验证成功
───────────────────────────────────────────────────────────────
```

#### 步骤 2: 模型选择

```
╔══════════════════════════════════════════════════════════════╗
║  配置向导 - 步骤 2/6: 模型选择                               ║
╚══════════════════════════════════════════════════════════════╝

  选择主模型

  ❯ Claude Sonnet 4.5 (推荐，平衡性能和成本)
    Claude Haiku 4 (经济型)
    Claude Opus 4.5 (最强性能)
    GPT-5 Mini (OpenAI)
    自定义模型

  💡 提示: Sonnet 适合大多数场景，Haiku 适合简单任务

───────────────────────────────────────────────────────────────
  选择备用模型 (可多选)

    [✓] Claude Haiku 4
    [✓] GPT-5 Mini
    [ ] DeepSeek Chat
───────────────────────────────────────────────────────────────
```

#### 步骤 3: 频道配置

```
╔══════════════════════════════════════════════════════════════╗
║  配置向导 - 步骤 3/6: 频道配置                               ║
╚══════════════════════════════════════════════════════════════╝

  选择要启用的频道

    [ ] WhatsApp (需要扫码登录)
  ❯ [✓] Telegram (需要 Bot Token)
    [ ] Discord (需要 Bot Token)
    [ ] 仅 Gateway (本地 API)

───────────────────────────────────────────────────────────────
  配置 Telegram Bot

  Bot Token: 123456789:ABC***
  
  ? 启用群组消息? [y/N]: y
  ? 群组中需要 @提及 才响应? [Y/n]: y
───────────────────────────────────────────────────────────────
```

#### 步骤 4: Gateway 配置

```
╔══════════════════════════════════════════════════════════════╗
║  配置向导 - 步骤 4/6: Gateway 配置                           ║
╚══════════════════════════════════════════════════════════════╝

  Gateway 端口 [18789]: 
  绑定地址 [127.0.0.1]: 
  
  ⚠️  警告: 绑定 0.0.0.0 会暴露到公网，请确保配置防火墙
  
  ? 自动生成 Gateway Token? [Y/n]: y
  
  ✓ Token 已生成: abc123***
───────────────────────────────────────────────────────────────
```

#### 步骤 5: 性能优化

```
╔══════════════════════════════════════════════════════════════╗
║  配置向导 - 步骤 5/6: 性能优化                               ║
╚══════════════════════════════════════════════════════════════╝

  Token 优化配置 (可节省 40-80% 成本)

  ❯ [✓] 限制上下文窗口 (50K tokens)
    [✓] 启用积极压缩
    [✓] 启用缓存优化
    [ ] 配置本地模型备用

───────────────────────────────────────────────────────────────
  Heartbeat 配置

  ❯ 30 分钟 (推荐)
    15 分钟 (高频)
    60 分钟 (低频)
    禁用
───────────────────────────────────────────────────────────────
```

#### 步骤 6: 确认并生成

```
╔══════════════════════════════════════════════════════════════╗
║  配置向导 - 步骤 6/6: 确认配置                               ║
╚══════════════════════════════════════════════════════════════╝

  ┌─────────────────────────────────────────────┐
  │ 配置摘要
  ├─────────────────────────────────────────────┤
  │   版本        中文版 (openclaw-cn)
  │   主模型      Claude Sonnet 4.5
  │   备用模型    Haiku, GPT-5 Mini
  │   频道        Telegram
  │   Gateway     127.0.0.1:18789
  │   优化        上下文限制, 缓存, 压缩
  │   Heartbeat   30 分钟
  └─────────────────────────────────────────────┘

  ? 确认生成配置文件? [Y/n]: y

  ⠹ 正在生成配置文件...
  ✓ 配置文件已生成: ~/.openclaw/openclaw.json
  ✓ 环境变量已保存: ~/.openclaw/.env
  ✓ 配置验证通过
```

---

### 模块 4: 技能管理 (skills.sh)

#### 功能描述
搜索、安装、管理 OpenClaw Skills。

#### 主界面

```
╔══════════════════════════════════════════════════════════════╗
║  🔧 技能管理                                                 ║
╚══════════════════════════════════════════════════════════════╝

  ❯ 🔍 搜索技能
    📦 已安装技能
    ⭐ 热门技能
    🔄 更新所有技能
    ← 返回主菜单

  ↑/↓ 选择  Enter 确认
```

#### 搜索技能

```
  🔍 搜索技能

  关键词: weather

  搜索结果 (3 个)
  ┌─────────────────────────────────────────────┐
  │ ❯ weather-forecast                          │
  │   获取天气预报信息                          │
  │   ⭐ 4.8  📥 1.2k  📦 已安装                │
  ├─────────────────────────────────────────────┤
  │   weather-alerts                            │
  │   天气预警通知                              │
  │   ⭐ 4.5  📥 800                            │
  ├─────────────────────────────────────────────┤
  │   weather-history                           │
  │   历史天气查询                              │
  │   ⭐ 4.2  📥 500                            │
  └─────────────────────────────────────────────┘

  ↑/↓ 选择  Enter 安装  i 详情  q 返回
```

#### 技能详情

```
  📦 技能详情: weather-forecast

  ┌─────────────────────────────────────────────┐
  │ 名称        weather-forecast                │
  │ 版本        1.2.0                           │
  │ 作者        openclaw-community              │
  │ 评分        ⭐⭐⭐⭐⭐ 4.8                    │
  │ 下载量      1,234                           │
  │ 更新时间    2026-01-15                      │
  ├─────────────────────────────────────────────┤
  │ 描述                                        │
  │ 获取全球天气预报，支持多城市、多天预报      │
  ├─────────────────────────────────────────────┤
  │ 依赖                                        │
  │ - API Key: OpenWeatherMap (免费)            │
  └─────────────────────────────────────────────┘

  [安装]  [查看源码]  [返回]
```

---

### 模块 5: 软件管理 (software.sh)

#### 功能描述
可选安装系统依赖软件，用户可自由选择。

#### 软件清单界面

```
╔══════════════════════════════════════════════════════════════╗
║  📦 软件安装                                                 ║
╚══════════════════════════════════════════════════════════════╝

  选择要安装的软件 (已检测系统: Ubuntu 24.04)

  ┌─ 必需组件 ─────────────────────────────────┐
  │ [✓] Node.js v22          已安装 ✓          │
  │ [✓] Git                  已安装 ✓          │
  │ [✓] curl                 已安装 ✓          │
  └─────────────────────────────────────────────┘

  ┌─ 推荐组件 ─────────────────────────────────┐
  │ [✓] jq (JSON 处理)       未安装            │
  │ [✓] ripgrep (快速搜索)   未安装            │
  │ [ ] fd-find (文件查找)   未安装            │
  └─────────────────────────────────────────────┘

  ┌─ 媒体工具 ─────────────────────────────────┐
  │ [✓] ffmpeg               未安装            │
  │ [ ] imagemagick          未安装            │
  │ [ ] tesseract-ocr        未安装            │
  └─────────────────────────────────────────────┘

  ┌─ 开发工具 ─────────────────────────────────┐
  │ [ ] Docker               未安装            │
  │ [ ] Python 3             已安装 ✓          │
  │ [ ] Chromium             未安装            │
  └─────────────────────────────────────────────┘

  已选择: 5 个软件  预计安装时间: 3 分钟

  ↑/↓ 移动  Space 选择  Enter 开始安装  s 跳过
```

#### 安装进度

```
  📦 正在安装软件...

  [1/5] jq
        ████████████████████████████████████████████████░░ 96%
        正在配置...

  [2/5] ripgrep                                        ✓ 完成
  [3/5] ffmpeg                                         ○ 等待
  [4/5] Node.js v22                                    ○ 跳过 (已安装)
  [5/5] Git                                            ○ 跳过 (已安装)

  ───────────────────────────────────────────────────────────
  总进度: 2/5 完成  预计剩余: 2 分钟
```

---

### 模块 6: 状态检查 (health.sh)

#### 功能描述
系统健康检查、诊断和修复。

#### 主界面

```
╔══════════════════════════════════════════════════════════════╗
║  🏥 系统状态检查                                             ║
╚══════════════════════════════════════════════════════════════╝

  ┌─ 服务状态 ─────────────────────────────────┐
  │ OpenClaw Gateway    ● 运行中  (PID: 12345) │
  │ 运行时间            3 天 2 小时 15 分钟    │
  │ 内存使用            256 MB / 2048 MB       │
  │ CPU 使用            2.3%                   │
  └─────────────────────────────────────────────┘

  ┌─ 配置状态 ─────────────────────────────────┐
  │ 配置文件            ✓ 有效                 │
  │ 环境变量            ✓ 已加载               │
  │ API Key             ✓ Anthropic, OpenAI    │
  │ 频道                ✓ Telegram (已连接)    │
  └─────────────────────────────────────────────┘

  ┌─ 资源使用 ─────────────────────────────────┐
  │ 磁盘空间            45 GB / 100 GB (45%)   │
  │ Session 大小        12 MB                  │
  │ 日志大小            156 MB                 │
  └─────────────────────────────────────────────┘

  [刷新]  [运行诊断]  [查看日志]  [返回]
```

#### 诊断结果

```
  🔍 运行诊断...

  ┌─ 诊断结果 ─────────────────────────────────┐
  │ ✓ Node.js 版本正常 (v22.5.0)              │
  │ ✓ OpenClaw CLI 版本正常 (1.3.0)           │
  │ ✓ 配置文件格式正确                        │
  │ ✓ Gateway 端口可用 (18789)                │
  │ ! Session 文件较大 (建议压缩)             │
  │ ✓ API Key 有效                            │
  │ ✓ 网络连接正常                            │
  └─────────────────────────────────────────────┘

  发现 1 个警告

  ? 是否自动修复? [Y/n]: y

  ⠹ 正在压缩 Session...
  ✓ Session 已压缩 (12 MB → 3 MB)
```

---

### 模块 7: 自更新 (updater.sh)

#### 功能描述
检查并更新部署脚本本身。

#### 更新检查

```
  🔄 检查更新...

  ┌─────────────────────────────────────────────┐
  │ 当前版本    2.0.0                           │
  │ 最新版本    2.1.0                           │
  │ 发布日期    2026-02-01                      │
  ├─────────────────────────────────────────────┤
  │ 更新内容                                    │
  │ - 新增: 多 Agent 路由支持                  │
  │ - 优化: 安装速度提升 30%                   │
  │ - 修复: WSL2 兼容性问题                    │
  └─────────────────────────────────────────────┘

  ? 是否更新到最新版本? [Y/n]: y

  ⠹ 正在下载更新...
  ████████████████████████████████████████████████░░ 96%

  ✓ 更新完成，请重新运行脚本
```

---

## 📊 主菜单设计

### 主界面

```
╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║     ██████╗ ██████╗ ███████╗███╗   ██╗ ██████╗██╗      █████╗║
║    ██╔═══██╗██╔══██╗██╔════╝████╗  ██║██╔════╝██║     ██╔══██║
║    ██║   ██║██████╔╝█████╗  ██╔██╗ ██║██║     ██║     ███████║
║    ██║   ██║██╔═══╝ ██╔══╝  ██║╚██╗██║██║     ██║     ██╔══██║
║    ╚██████╔╝██║     ███████╗██║ ╚████║╚██████╗███████╗██║  ██║
║     ╚═════╝ ╚═╝     ╚══════╝╚═╝  ╚═══╝ ╚═════╝╚══════╝╚═╝  ╚═╝
║                                                              ║
║              🦞 智能一键部署系统 v2.0                         ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

  当前状态: OpenClaw 1.3.0 (中文版) ● 运行中

  ❯ 🚀 安装 / 升级 OpenClaw
    ⚙️  系统配置向导
    🔧 技能管理
    📦 软件安装
    🏥 系统状态检查
    🔄 检查更新
    ❓ 帮助
    🚪 退出

  ↑/↓ 选择  Enter 确认  q 退出
```

### 首次运行界面

```
╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║     ██████╗ ██████╗ ███████╗███╗   ██╗ ██████╗██╗      █████╗║
║    ██╔═══██╗██╔══██╗██╔════╝████╗  ██║██╔════╝██║     ██╔══██║
║    ██║   ██║██████╔╝█████╗  ██╔██╗ ██║██║     ██║     ███████║
║    ██║   ██║██╔═══╝ ██╔══╝  ██║╚██╗██║██║     ██║     ██╔══██║
║    ╚██████╔╝██║     ███████╗██║ ╚████║╚██████╗███████╗██║  ██║
║     ╚═════╝ ╚═╝     ╚══════╝╚═╝  ╚═══╝ ╚═════╝╚══════╝╚═╝  ╚═╝
║                                                              ║
║              🦞 智能一键部署系统 v2.0                         ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

  👋 欢迎使用 OpenClaw 智能部署系统！

  检测到这是首次运行，将引导您完成安装。

  ┌─────────────────────────────────────────────┐
  │ 系统环境                                    │
  │   操作系统    Ubuntu 24.04 LTS              │
  │   内存        8192 MB ✓                     │
  │   Node.js     未安装                        │
  │   OpenClaw    未安装                        │
  └─────────────────────────────────────────────┘

  ? 开始安装向导? [Y/n]: 
```

---

## 🗓️ 开发计划

### 第一阶段: 基础框架 (3-4 天)

| 任务 | 描述 | 优先级 | 预计时间 |
|------|------|--------|----------|
| UI 框架 | 实现所有 UI 组件 | P0 | 1 天 |
| 工具函数 | 日志、配置、网络等 | P0 | 0.5 天 |
| 环境检测 | 系统和依赖检测 | P0 | 1 天 |
| 主入口 | 主菜单和路由 | P0 | 0.5 天 |
| 测试 | 基础功能测试 | P0 | 1 天 |

**交付物**:
- `lib/ui.sh` - UI 框架
- `lib/utils.sh` - 工具函数
- `lib/detector.sh` - 环境检测
- `deploy.sh` - 主入口

### 第二阶段: 核心功能 (4-5 天)

| 任务 | 描述 | 优先级 | 预计时间 |
|------|------|--------|----------|
| 安装管理 | 双版本安装支持 | P0 | 1.5 天 |
| 配置向导 | 交互式配置 | P0 | 2 天 |
| 软件管理 | 依赖软件安装 | P1 | 1 天 |
| 测试 | 核心功能测试 | P0 | 0.5 天 |

**交付物**:
- `lib/installer.sh` - 安装管理
- `lib/wizard.sh` - 配置向导
- `lib/software.sh` - 软件管理

### 第三阶段: 扩展功能 (3-4 天)

| 任务 | 描述 | 优先级 | 预计时间 |
|------|------|--------|----------|
| 技能管理 | 搜索安装技能 | P1 | 1.5 天 |
| 状态检查 | 健康检查诊断 | P1 | 1 天 |
| 自更新 | 脚本自更新 | P2 | 0.5 天 |
| 测试 | 完整测试 | P1 | 1 天 |

**交付物**:
- `lib/skills.sh` - 技能管理
- `lib/health.sh` - 状态检查
- `lib/updater.sh` - 自更新

### 第四阶段: 优化完善 (2-3 天)

| 任务 | 描述 | 优先级 | 预计时间 |
|------|------|--------|----------|
| 错误处理 | 完善错误处理 | P1 | 0.5 天 |
| 文档 | 使用文档 | P1 | 0.5 天 |
| 单文件打包 | 合并为单文件 | P2 | 0.5 天 |
| 兼容性测试 | 多平台测试 | P1 | 1 天 |
| 发布 | 版本发布 | P1 | 0.5 天 |

**交付物**:
- 完整的部署脚本
- 使用文档
- 发布版本

---

## 📋 开发检查清单

### UI 框架
- [ ] 颜色方案定义
- [ ] Banner 显示
- [ ] 进度条组件
- [ ] 旋转动画组件
- [ ] 单选菜单组件
- [ ] 多选菜单组件
- [ ] 输入框组件
- [ ] 确认框组件
- [ ] 信息面板组件
- [ ] 状态指示器

### 环境检测
- [ ] 操作系统检测
- [ ] 系统资源检测
- [ ] 依赖检测
- [ ] OpenClaw 检测
- [ ] 网络检测
- [ ] 安装模式判断

### 安装管理
- [ ] 版本选择
- [ ] 安装方式选择
- [ ] 系统依赖安装
- [ ] Node.js 安装
- [ ] OpenClaw CLI 安装
- [ ] 服务配置
- [ ] 安装验证

### 配置向导
- [ ] Provider 配置
- [ ] 模型选择
- [ ] 频道配置
- [ ] Gateway 配置
- [ ] 性能优化配置
- [ ] 配置确认生成
- [ ] 配置验证

### 技能管理
- [ ] 技能搜索
- [ ] 技能安装
- [ ] 已安装列表
- [ ] 技能更新
- [ ] 技能详情

### 软件管理
- [ ] 软件清单显示
- [ ] 已安装检测
- [ ] 批量安装
- [ ] 安装进度显示

### 状态检查
- [ ] 服务状态
- [ ] 配置状态
- [ ] 资源使用
- [ ] 诊断功能
- [ ] 自动修复

### 自更新
- [ ] 版本检查
- [ ] 更新下载
- [ ] 更新安装

---

## 🎯 成功指标

### 用户体验
- **安装成功率**: > 95%
- **平均安装时间**: < 10 分钟
- **用户满意度**: > 90%

### 技术指标
- **代码覆盖率**: > 80%
- **错误处理率**: 100%
- **兼容性**: Ubuntu/Debian/CentOS/macOS/WSL2

### 功能完整性
- **核心功能**: 100% 实现
- **扩展功能**: 100% 实现
- **文档完整性**: 100%

---

## 📝 备注

### 技术选型
- **语言**: Bash (兼容性最好)
- **最低 Bash 版本**: 4.0
- **依赖**: curl, jq (可选)

### 兼容性目标
- Ubuntu 20.04+
- Debian 11+
- CentOS 8+
- macOS 12+
- Windows (WSL2)

### 参考资源
- 官方文档: https://docs.openclaw.ai/
- 中文文档: https://clawd.org.cn/
- ClawHub: https://clawhub.com/

---

**文档结束** - 准备开始开发！🚀
